{
  "name": "texas-stores-management",
  "version": "1.0.0",
  "description": "Daily profit dashboard + BOM/pricing management for Chillers POS",
  "main": "src/server.js",
  "scripts": {
    "postinstall": "prisma generate",
    "start": "npx prisma db push --accept-data-loss && node prisma/seed.js && node src/server.js",
    "seed": "node prisma/seed.js",
    "dev": "node src/server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
